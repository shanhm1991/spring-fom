<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
			"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="oracle.demo">
	<select id="selectDemo" resultType="Map">
		select * from demo
	</select>

	<insert id="insertDemo" parameterType="demo">
		insert into demo(id,name,age,tel) values (#{id},#{name},#{age},#{tel})
	</insert>

	<insert id="batchInsertDemo" parameterType="List">
		insert into demo(id,name,age,tel) values
		<foreach collection="list" item="item" index="index" separator=",">
			(#{item.id},#{item.name},#{item.age},#{item.tel})
		</foreach>
	</insert>
</mapper>